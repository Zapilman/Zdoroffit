export const enum Routes {
	WORKOUT = '(tabs)',

	EXERCISES = '(stack)/(top-tabs)',
	EXERCISE_HISTORY = '(stack)/exercise-history',
	SAVED_PROGRAMS = '(stack)/saved-programs',

	PROFILE = '(stack)/profile',
	THEME_SETTINGS = `${PROFILE}/theme`,
}
